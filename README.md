# Concierge

Personal AI orchestration system (control plane).

## Installation

```bash
pip install -e .
```

## Usage

```bash
bit init /path/to/workspace
bit ws validate --path /path/to/workspace
bit ws open --path /path/to/workspace
```
